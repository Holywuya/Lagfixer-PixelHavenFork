AbilityLimiter:
  enabled: true
  worlds: # 使用 '*' 启用所有世界。
    - '*'
  values:
    elytra_boost:
      cooldown: 5
      # 此选项已被开发者禁用
      speed_multiplier: 0.85
      # 设为 0 以禁用
      additional_durability_loss: 5

    trident_riptide:
      cooldown: 3
      # 此选项已被开发者禁用
      speed_multiplier: 0.85
      # 设为 0 以禁用
      additional_durability_loss: 5
