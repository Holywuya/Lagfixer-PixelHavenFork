# 需要重启服务器才能应用更改。
# 通过替换为新的优化对象来优化碰撞。
VehicleMotionReducer:
  enabled: false
  worlds: # 使用 '*' 启用所有世界。
    - '*'
  values:
    # 实验性功能
    force_load: false

    minecart:
      enabled: true
      # 移除自然生成的矿车，适用于频繁生成区块的服务器。
      remove_chest: false

    boat:
      enabled: true
